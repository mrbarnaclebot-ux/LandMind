---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/vite.config.ts
  - packages/client/index.html
  - packages/client/src/main.tsx
  - packages/client/src/App.tsx
  - packages/client/src/App.css
  - packages/client/src/scene/BabylonScene.tsx
autonomous: true

must_haves:
  truths:
    - "Vite dev server starts and serves the application"
    - "Browser displays a Babylon.js 3D scene with camera and lighting"
    - "User can interact with the scene (orbit camera with mouse)"
    - "No console errors related to Babylon.js initialization"
  artifacts:
    - path: "packages/client/src/scene/BabylonScene.tsx"
      provides: "Babylon.js scene component with camera and lighting"
      contains: "arcRotateCamera"
    - path: "packages/client/src/App.tsx"
      provides: "Root React component mounting Babylon scene"
      contains: "BabylonScene"
    - path: "packages/client/vite.config.ts"
      provides: "Vite configuration for React + TypeScript"
      contains: "@vitejs/plugin-react"
    - path: "packages/client/index.html"
      provides: "HTML entry point"
      contains: "root"
  key_links:
    - from: "packages/client/src/App.tsx"
      to: "packages/client/src/scene/BabylonScene.tsx"
      via: "React component import"
      pattern: "import.*BabylonScene"
    - from: "packages/client/src/main.tsx"
      to: "packages/client/src/App.tsx"
      via: "React root render"
      pattern: "import App"
---

<objective>
Create Vite + React frontend with Babylon.js empty scene rendering.

Purpose: Establishes the 3D rendering foundation for the hex world visualization. This is independent of the backend and can be developed in parallel.

Output: Browser application at localhost:5173 displaying a 3D scene with camera controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

Research provides Babylon.js + React integration patterns and Vite configuration.
This plan is independent of backend infrastructure (Wave 1).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React project</name>
  <files>
    packages/client/package.json
    packages/client/tsconfig.json
    packages/client/vite.config.ts
    packages/client/index.html
  </files>
  <action>
1. Create packages/client directory if it doesn't exist.

2. Create packages/client/package.json:
   ```json
   {
     "name": "@landmind/client",
     "version": "0.0.1",
     "private": true,
     "type": "module",
     "scripts": {
       "dev": "vite",
       "build": "tsc -b && vite build",
       "preview": "vite preview"
     },
     "dependencies": {
       "@babylonjs/core": "^8.0.0",
       "@babylonjs/inspector": "^8.0.0",
       "react": "^19.0.0",
       "react-babylonjs": "^3.2.0",
       "react-dom": "^19.0.0"
     },
     "devDependencies": {
       "@types/react": "^19.0.0",
       "@types/react-dom": "^19.0.0",
       "@vitejs/plugin-react": "^4.3.0",
       "typescript": "^5.7.0",
       "vite": "^6.0.0"
     }
   }
   ```

3. Create packages/client/tsconfig.json:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "lib": ["ES2022", "DOM", "DOM.Iterable"],
       "module": "ESNext",
       "skipLibCheck": true,
       "moduleResolution": "bundler",
       "allowImportingTsExtensions": true,
       "isolatedModules": true,
       "moduleDetection": "force",
       "noEmit": true,
       "jsx": "react-jsx",
       "strict": true,
       "noUnusedLocals": true,
       "noUnusedParameters": true,
       "noFallthroughCasesInSwitch": true,
       "noUncheckedSideEffectImports": true
     },
     "include": ["src"]
   }
   ```

4. Create packages/client/vite.config.ts:
   ```typescript
   import { defineConfig } from 'vite';
   import react from '@vitejs/plugin-react';

   export default defineConfig({
     plugins: [react()],
     server: {
       port: 5173,
     },
   });
   ```

5. Create packages/client/index.html:
   ```html
   <!DOCTYPE html>
   <html lang="en">
     <head>
       <meta charset="UTF-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <title>LandMind</title>
     </head>
     <body>
       <div id="root"></div>
       <script type="module" src="/src/main.tsx"></script>
     </body>
   </html>
   ```

6. Run npm install from project root to install client dependencies.
  </action>
  <verify>
Run `npm install` from project root - should install client dependencies.
Run `ls packages/client/node_modules/@babylonjs` - should show core and inspector.
  </verify>
  <done>
Vite React project initialized with Babylon.js dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create React app with Babylon.js scene</name>
  <files>
    packages/client/src/main.tsx
    packages/client/src/App.tsx
    packages/client/src/App.css
    packages/client/src/scene/BabylonScene.tsx
  </files>
  <action>
1. Create packages/client/src directory structure:
   ```
   packages/client/src/
   ├── main.tsx
   ├── App.tsx
   ├── App.css
   └── scene/
       └── BabylonScene.tsx
   ```

2. Create packages/client/src/main.tsx:
   ```tsx
   import { StrictMode } from 'react';
   import { createRoot } from 'react-dom/client';
   import App from './App';
   import './App.css';

   createRoot(document.getElementById('root')!).render(
     <StrictMode>
       <App />
     </StrictMode>
   );
   ```

3. Create packages/client/src/App.css:
   ```css
   * {
     margin: 0;
     padding: 0;
     box-sizing: border-box;
   }

   html, body, #root {
     width: 100%;
     height: 100%;
     overflow: hidden;
   }

   #babylon-canvas {
     width: 100%;
     height: 100%;
     touch-action: none;
   }
   ```

4. Create packages/client/src/App.tsx:
   ```tsx
   import { BabylonScene } from './scene/BabylonScene';

   function App() {
     return <BabylonScene />;
   }

   export default App;
   ```

5. Create packages/client/src/scene/BabylonScene.tsx:
   ```tsx
   import { Engine, Scene } from 'react-babylonjs';
   import { Vector3, Color3 } from '@babylonjs/core';

   export function BabylonScene() {
     return (
       <Engine antialias adaptToDeviceRatio canvasId="babylon-canvas">
         <Scene clearColor={new Color3(0.1, 0.1, 0.15).toColor4()}>
           <arcRotateCamera
             name="camera"
             alpha={Math.PI / 2}
             beta={Math.PI / 4}
             radius={10}
             target={Vector3.Zero()}
             minZ={0.1}
             wheelPrecision={50}
           />
           <hemisphericLight
             name="light"
             intensity={0.7}
             direction={Vector3.Up()}
           />
           {/* Ground plane for visual reference */}
           <ground
             name="ground"
             width={20}
             height={20}
             subdivisions={2}
           >
             <standardMaterial name="groundMat" specularColor={Color3.Black()}>
               <texture
                 assignTo="diffuseTexture"
                 url="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=="
               />
             </standardMaterial>
           </ground>
         </Scene>
       </Engine>
     );
   }
   ```

Note: The ground plane with a simple 1px texture provides a visual reference point. This will be replaced with the hex grid in Phase 2.
  </action>
  <verify>
Run `npm run client` from project root (or `npm run dev` from packages/client).
Open http://localhost:5173 in browser - should show 3D scene with ground plane.
  </verify>
  <done>
React app renders with Babylon.js scene showing camera, light, and ground reference.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify scene interactivity and clean startup</name>
  <files>
    (none - verification task)
  </files>
  <action>
1. Start the development server:
   ```bash
   npm run client
   ```

2. Open http://localhost:5173 in a browser

3. Verify the following:
   - Scene renders without console errors
   - Dark background color (0.1, 0.1, 0.15) is visible
   - Ground plane is visible at origin
   - Mouse drag rotates the camera (orbit)
   - Mouse wheel zooms in/out
   - Window resize updates canvas size

4. Open browser DevTools:
   - Check Console for any errors (should be none)
   - Check Network tab - all resources should load

5. Test in multiple browsers if available (Chrome, Firefox, Safari)

6. Stop dev server when verification is complete
  </action>
  <verify>
Browser shows 3D scene, camera controls work, no console errors.
  </verify>
  <done>
Babylon.js scene renders interactively in browser with no errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run client` starts Vite dev server on port 5173
2. Browser at http://localhost:5173 shows 3D scene
3. Mouse drag orbits camera around scene
4. Mouse wheel zooms in/out
5. Browser console shows no errors
6. Canvas resizes with window
</verification>

<success_criteria>
- Vite dev server starts without errors
- Browser displays Babylon.js 3D scene
- Arc rotate camera responds to mouse input (orbit, zoom)
- Scene includes hemispheric lighting
- No JavaScript errors in browser console
- Canvas fills the browser viewport
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
