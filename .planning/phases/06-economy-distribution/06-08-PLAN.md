---
phase: 06-economy-distribution
plan: 08
type: execute
wave: 5
depends_on: ["06-06", "06-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Fee vault is initialized on devnet"
    - "Earnings dashboard shows real claimable amount"
    - "Leaderboard updates as agents mine"
    - "Heat map toggles and shows resource distribution"
    - "Claim flow completes successfully (if sufficient balance)"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of Phase 6 economic features.

Purpose: Validates that all economic components work together - vault initialization, earnings tracking, leaderboard updates, heat map visualization, and claim flow.
Output: Verification checklist completed, phase ready for UAT.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-economy-distribution/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize vault and verify contract deployment</name>
  <files></files>
  <action>
Run the vault initialization script on devnet:

1. Ensure SERVER_WALLET_SECRET is set in .env with funded devnet wallet
2. Run: `npm run init-vault` from packages/server
3. Verify vault_state account was created
4. Check vault_state address matches expected PDA

Verification steps:
```bash
# From packages/server
npm run init-vault

# Verify with solana CLI
solana account <vault_state_address> --output json
```

If already initialized, script should skip and report existing address.

Also verify the treasury PDA has some balance (from prior agent deployments):
```bash
solana balance <treasury_pda>
```
  </action>
  <verify>Vault state account exists on devnet; treasury has balance from deployments</verify>
  <done>Fee vault initialized and ready for claims</done>
</task>

<task type="auto">
  <name>Task 2: Verify earnings dashboard and leaderboard</name>
  <files></files>
  <action>
Start the application and test earnings features:

1. Start server: `npm run dev` from packages/server
2. Start client: `npm run dev` from packages/client
3. Connect wallet (ensure wallet has deployed agents mining)
4. Open Earnings Dashboard via toggle button

**Verify:**
- [ ] Dashboard opens with slide animation
- [ ] Weighted score displayed (should be > 0 if mining)
- [ ] Claimable amount shown (may be 0 initially)
- [ ] Rank displayed (e.g., "#5 / 12 miners")
- [ ] Percentile shown
- [ ] Resource totals match Agent Dashboard totals
- [ ] Leaderboard shows top miners (or placeholder if no data)

**Test socket updates:**
- Wait for mining tick (~5 seconds)
- Observe if weighted score increases
- Check console for socket events

**API verification:**
```bash
# Get session cookie first, then:
curl -H "Cookie: session=..." http://localhost:3001/api/earnings
curl http://localhost:3001/api/leaderboard
```
  </action>
  <verify>Dashboard displays real data; leaderboard shows rankings; socket updates working</verify>
  <done>Earnings dashboard and leaderboard verified functional</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 6 economic features:
- Fee vault smart contract with claim, pause, merkle root instructions
- Earnings calculation using weighted resource multipliers
- Leaderboard with Redis ZSET for real-time rankings
- Earnings Dashboard UI with claim flow
- Heat map overlay showing resource concentration
  </what-built>
  <how-to-verify>
1. **Connect wallet** and ensure you have mining agents deployed

2. **Open Earnings Dashboard** (click EARNINGS button)
   - Verify weighted score is displayed and > 0
   - Verify rank shows your position
   - Verify leaderboard shows top 10 (or your rank if no others)

3. **Mine for a few ticks** (wait ~30 seconds)
   - Watch weighted score increase in dashboard
   - Verify leaderboard position updates if score changes

4. **Toggle Heat Map** (click HEAT MAP button)
   - Gold hexes should appear red/orange (hot)
   - Iron hexes should appear blue (cold)
   - Empty hexes minimal color
   - Toggle off - colors should fade out

5. **Claim Flow** (only if claimable >= 0.025 SOL)
   - Click Claim button
   - Confirmation dialog should appear
   - If you proceed, transaction should build, sign, and confirm
   - Balance should update after claim

6. **Test edge cases:**
   - Claim button disabled when < 0.025 SOL
   - Dashboard shows "0.0000 SOL" claimable if no fees deposited
   - Leaderboard shows user rank even if not in top 10

**Expected result:** All economic features work, data is accurate, UX is smooth.
  </how-to-verify>
  <resume-signal>
Type "approved" if all features work correctly.

If issues found, describe:
- What feature failed
- Expected vs actual behavior
- Console errors if any
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] Vault state account exists on devnet
- [ ] Treasury PDA has balance
- [ ] Earnings dashboard opens and displays data
- [ ] Weighted score reflects mined resources
- [ ] Leaderboard shows rankings
- [ ] Heat map toggles on/off with fade
- [ ] Heat map colors match resource types
- [ ] Socket events update UI in real-time
- [ ] Claim flow works (if sufficient balance)
</verification>

<success_criteria>
Phase 6 verification complete:
1. All smart contract instructions deployed and functional
2. Earnings calculation accurate (weighted by resource rarity)
3. Leaderboard reflects current mining standings
4. Heat map visualizes resource concentration
5. Claim flow executes successfully
6. UI updates in real-time via socket events
7. Human verification confirms UX quality
</success_criteria>

<output>
After completion, create `.planning/phases/06-economy-distribution/06-08-SUMMARY.md`
</output>
