{
  "name": "landmind",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "npm run dev --workspace=@landmind/server",
    "client": "npm run dev --workspace=@landmind/client"
  }
}
